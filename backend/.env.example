# Copy to .env and fill in. The analyze endpoint uses Claude Opus 4.8.
ANTHROPIC_API_KEY=sk-ant-...

# Optional: a read-only GitHub token raises the public-repo API rate limit.
# Not required — public repos work unauthenticated.
GITHUB_TOKEN=
